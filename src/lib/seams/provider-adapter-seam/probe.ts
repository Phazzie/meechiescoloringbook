/*
 * Purpose: Probe entry point for ProviderAdapterSeam.
 * Why: Allow verification tooling to probe external AI provider connectivity.
 * Info flow: Probe execution -> probe result -> evidence capture.
 * Invariants: Real provider probes require XAI_API_KEY.
 */
// N/A (pure stub): ProviderAdapterSeam is probed via probes/provider-adapter.probe.mjs.
export {};
