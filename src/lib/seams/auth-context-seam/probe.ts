/*
 * Purpose: Probe entry point for AuthContextSeam.
 * Why: Allow verification tooling to probe auth context lifecycle behavior.
 * Info flow: Probe execution -> probe result -> evidence capture.
 * Invariants: AuthContextSeam is probed via probes/browser-seams.probe.mjs.
 */
// N/A (browser-only): AuthContextSeam is probed via probes/browser-seams.probe.mjs.
export {};
