/*
 * Purpose: Probe entry point for CreationStoreSeam.
 * Why: Allow verification tooling to probe browser creation storage lifecycle behavior.
 * Info flow: Probe execution -> probe result -> evidence capture.
 * Invariants: CreationStoreSeam is probed via probes/browser-seams.probe.mjs.
 */
// N/A (browser-only): CreationStoreSeam is probed via probes/browser-seams.probe.mjs.
export {};
