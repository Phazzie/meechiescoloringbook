// Purpose: Probe DriftDetectionSeam against real inputs.
// Why: Verify the adapter produces expected contract outputs.
// Info flow: probe input -> adapter -> fixture comparison.
// N/A (pure): DriftDetectionSeam is deterministic and has no external I/O.
export {};
