/*
 * Purpose: Probe entry point for ChatInterpretationSeam.
 * Why: Allow verification tooling to probe chat interpretation behavior.
 * Info flow: Probe execution -> probe result -> evidence capture.
 * Invariants: Live probing requires network and API server.
 */
// N/A (server/browser composition): ChatInterpretationSeam is probed via probes/chat-interpretation.probe.mjs.
export {};
