# Purpose: Enforce consistent package manager behavior.
# Why: Prevent dependency resolution drift across environments.
# Info flow: npm reads config -> install/build behaves deterministically.

engine-strict=true
