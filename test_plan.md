Plan: Make UI A+ Gold Medal
1. Add kinetic elements (transitions) to all interactive elements.
2. Update primary buttons with animated background gradients.
3. Enhance paper realism with a subtle SVG noise texture, drop shadow, and transform.
4. Add Svelte transitions (`fade`, `fly`) to the verdict and generated image.
5. Add a glowing effect for mode card active states and input focus states.
6. Check `npm run check` and `npm run verify` to ensure SDD compliance.
