# Purpose: Record the whole verification sequence for this head in one place — the eight-stage
#          chain, the standalone checks run around it, and what each artifact in this folder does
#          and does not prove.
# Why: The individual artifacts each show one command's output. Nothing else in the folder says in
#      what order they were produced, which of them the chain wrote and which were run by hand, or
#      why a file carries a freshness marker. Two wrong explanations of that marker have already
#      shipped in this folder; this file is where the corrections live.
# Info flow: npm run verify + standalone checks -> their evidence files -> this summary -> review.
#
# npm run verify — Run 3 (worst-feature routine), close-out head after the merge of PR #293
# Chain run at 2026-09-04T15:44:08.138Z
#
# This head is `main` at cccf0c2 (the merge of PR #293) plus documentation commits. The src/ and
# tests/ trees are byte-identical to that merge commit; the only changes are WORST_TO_BEST_LOG.md
# and the evidence artifacts in this folder.
#
# Change under test: the Run 3 close-out entry in WORST_TO_BEST_LOG.md, corrected across three
# review rounds on the entry itself.
#
# Seams touched: none. No file under contracts/, probes/, fixtures/, src/lib/mocks/,
# tests/contract/, src/lib/adapters/ or src/lib/seams/ is in the diff. No contract shape changed.
# cipher-gate.json therefore correctly predates this run.

ALL EIGHT STAGES RAN. EXIT 0.

  1. npm run audit:gate      found 0 vulnerabilities
  2. chamber-lock.mjs        ok
  3. verify-runner.mjs       check: 0 errors, 0 warnings | test: 1173 passed, 1 skipped
  4. shaolin-lint.mjs        ok (0 stale, 0 missing)
  5. assumption-alarm.mjs    ok
  6. seam-ledger.mjs         ok
  7. clan-chain.mjs          ok
  8. proof-tape.mjs          ok

Run on this head after the chain, then `npm run proof:tape` once more so the tape sees them:

  npm run lint        exit 0, clean                  -> lint.txt
  npm run build       exit 0                         -> build.txt
  npx playwright test 22 passed                      -> e2e.txt

And by the chain itself:

  npm run check       0 errors, 0 warnings
  npm test            1173 passed | 1 skipped        -> test.txt

## Why the standalone checks were re-run for a documentation-only commit

The first push of this close-out left lint.txt and build.txt as they were, on the argument that the
src/ and tests/ trees are byte-identical to the merge commit those artifacts already describe, and
that a fresh timestamp over unchanged output is not evidence of anything.

Codex raised it as a P1 and is right, for a reason the argument missed. AGENTS.md requires
`npm run check`, `npm run lint`, `npm test` and `npm run build` **before every push** — the rule is
about the head being pushed, not about whether the inputs are believed to have changed. An artifact
that predates the head cannot prove the head passed, however sound the reasoning is that it would.
"I am sure it still passes" is a claim; running it is a measurement.

The e2e suite is scoped by AGENTS.md to user-facing changes, and this one is not. It was run anyway
rather than left describing an earlier head, because the argument for leaving it is the same
argument that was already wrong once above.

## How to make proof-tape.md see them, and a correction to two earlier claims

`markArtifactsPredatingRun` in scripts/proof-tape.mjs takes its marker from the mtime of
chamber-lock.json, written at **stage 2** of the chain. Two claims about that have now been wrong in
this folder:

  - The previous run's note said running lint and build *after* the chain makes them postdate the
    marker. Half true: they do postdate it, but proof-tape.md was already rendered at stage 8 and
    does not see them, so the tape still marks them.
  - This file's own previous revision then concluded they can therefore **never** come out fresh.
    That is also wrong, and Codex named the fix: run them after the chain and then invoke
    `npm run proof:tape` on its own. chamber-lock.json keeps its stage-2 mtime, the standalone
    artifacts are newer than it, and the re-rendered tape reports them as current.

That is the order used here. cipher-gate.json is now the only marked file, and it is marked
correctly: no seam artifact is in this diff, so no Cipher Gate entry was required or written.

A note on the shape of that mistake, since this run's log entry is about exactly it: the first claim
was inherited from the previous run's file and repeated without testing. The second was reasoned
from the source and stated as an impossibility after one attempt. Reading the code is not the same
as running it either.
