# Purpose: Record the whole verification sequence for this head in one place — the eight-stage
#          chain, the standalone checks run around it, and what each artifact in this folder does
#          and does not prove.
# Why: The individual artifacts each show one command's output. Nothing else in the folder says in
#      what order they were produced, which of them the chain wrote and which were run by hand, or
#      why a file carries a freshness marker.
# Info flow: npm run verify + standalone checks -> their evidence files -> this summary -> review.
#
# npm run verify — Run 4 (worst-feature routine): /m/[mode] becomes a real mode page
# When the chain last ran: read chamber-lock.json's generatedAt. **This header no longer states it.**
# It used to copy that value, and the copy went stale twice — once carrying a 19:07 run forward
# through two later executions, and again in round 19, when the reordering moved this file's writing
# *before* the chain and the copied timestamp silently became the previous run's. A value copied out
# of an artifact is a claim about that artifact; the artifact is closer to hand than this sentence.
#
# Change under test: the focused-mode pages behind /m/<slug> — the only page five of the eight
# modes have, and where every focused-mode link on the home page lands — stop dead-ending at a
# verdict and become full coloring-page factories, sharing VerdictPageState and VerdictPageStudio
# with the three standalone mode routes. Plus a mode catalog derived from studioModes, a 404 for an
# unknown slug in place of a silent fallback to Random Meechie, and the app's first error page.
#
# Seams touched: none. No file under contracts/, probes/, fixtures/, src/lib/mocks/,
# tests/contract/, src/lib/adapters/ or src/lib/seams/ is in the diff, and no contract shape
# changed. Every seam the rebuilt page reaches is called through adapters that already existed —
# fourteen of them, not the six this header listed for most of this run. The authoritative
# enumeration, its partition into browser / generate-path / nested, and the reason five of them are
# additionally run under a canonical "(self-contained)" row all live in the Round 5 section below;
# this header deliberately does not restate the list, because a second copy is a second thing to go
# stale and that is exactly how it was wrong here.
#
# **proof-tape flags THIS FILE, and its label for the flag is wrong here — but the fix is a script
# change, not a wording change, so this run records it rather than papering over it.**
# The tape prints "written by an earlier run ... read as history, not as proof of the current
# change". That is false for this file: it was written deliberately, minutes before the chain, so
# the chain would cover it (round 19's finding). But `markArtifactsPredatingRun` in
# scripts/proof-tape.mjs is `modified < chamber-lock.json's mtime` and nothing else, so **any
# summary the chain covers is necessarily flagged** — the two properties cannot both hold, and the
# marker has no way to tell "input written before the chain on purpose" from "leftover from another
# run". Both readings are in this directory right now and they contradict each other.
# This run picks chain coverage and leaves the flag standing, because the alternative is editing
# scripts/proof-tape.mjs inside a documentation close-out. **Follow-up 11: give the marker a way to
# distinguish deliberate pre-chain inputs from stale leftovers** — an explicit list, a manifest, or
# moving hand-written summaries out of the inventoried set. Until then: any *other* file flagged
# here is worth investigating; this one is expected, and the tape's sentence about it is not.
# The history, since this header asserted otherwise for several rounds: cipher-gate.json was the
# flagged artifact through rounds 4 and 5 (correctly), stopped being flagged in round 6, and the
# header's "marks nothing as stale" line survived into round 19's reordering, where it became
# false.

ALL EIGHT STAGES RAN. EXIT 0.

  1. npm run audit:gate      found 0 vulnerabilities
  2. chamber-lock.mjs        ok
  3. verify-runner.mjs       check: 0 errors, 0 warnings | test: 1187 passed, 1 skipped
  4. shaolin-lint.mjs        ok
  5. assumption-alarm.mjs    ok
  6. seam-ledger.mjs         ok
  7. clan-chain.mjs          ok
  8. proof-tape.mjs          ok

Run on this head after the chain, then `npm run proof:tape` once more so the tape sees them:

  npm run lint        exit 0, clean                  -> lint.txt
  npm run build       exit 0                         -> build.txt
  npx playwright test 28 passed                      -> e2e.txt

And by the chain itself:

  npm run check       0 errors, 0 warnings
  npm test            1187 passed | 1 skipped        -> test.txt

Baseline on main at 210b301, for comparison: 1173 unit tests passed | 1 skipped, 22 e2e passed.
This head adds 14 unit tests (tests/unit/mode-catalog.test.ts) and 6 e2e tests.

## The ordering rule for the standalone artifacts, which still holds

`markArtifactsPredatingRun` in scripts/proof-tape.mjs takes its marker from the mtime of
chamber-lock.json, written at **stage 2** of the chain. Running lint, build and the e2e suite after
the chain makes them newer than that marker, but proof-tape.md was already rendered at stage 8 and
does not see them — so `npm run proof:tape` is invoked once more on its own afterwards. That order
was used here: chain, then the three standalone checks, then the tape.

## A note on the browser used for the e2e run

This container ships Chromium build 1194; the pinned @playwright/test resolves build 1208, and
downloading browsers is not available in this environment. The 1208 headless-shell path was
symlinked to the installed 1194 binary so the suite could run. `playwright.config.ts` is unchanged
and is not part of this diff — CI resolves its own browser in the normal way, so this affects the
local evidence run only and nothing about how the suite executes in CI.

## What the e2e suite proves that the unit tests cannot

Three of this change's guards are only observable in a browser, and each was confirmed by deleting
the guard and watching the test fail rather than by reading the code. This said "two" for most of
this run and omitted the third, which is a coverage claim a later audit would have read as complete:

  - The `{#key config.slug}` in src/routes/m/[mode]/+page.svelte. SvelteKit reuses one component
    instance across parameter changes on the same route, so without the key the previous mode's
    verdict survives into the next mode. The first version of this test navigated with `page.goto`,
    which builds a new document, and so passed with the key deleted — it proved nothing. It now
    walks an in-app link, marks the document, and asserts the mark survives the click, so it fails
    if the navigation ever stops being client-side.
  - The 404 for an unknown slug, which is a response status and not a rendered value.
  - The 32px horizontal overflow at a 390px viewport. `document.documentElement.scrollWidth`
    against `clientWidth` is a layout measurement, and jsdom does no layout, so no unit test can
    make this assertion at all. Removing the `overflow-x: clip` fails with
    "/m/who-fucked-up pans sideways by 32px".

  The dedication-split test is deliberately **not** in this list. It is about component state after
  a second verdict, which a component test with a DOM could assert; it runs in the e2e suite because
  that is where the verdict flow is already stubbed, not because a browser is required for it.

## Round 2 — the SonarCloud duplication gate, and what it was actually pointing at

SonarCloud's Quality Gate failed on the first head `75b0017`: **4.5% Duplication on New Code,
required <= 3%**. It is a measure rather than an issue, so it posts no check-run annotations and the
duplicated block has to be found by hand.

The first guess was wrong and is worth recording. The obvious suspect was the CSS in
`MeechieModePage.svelte`, which mirrors the three standalone mode routes — ten rule blocks are
byte-identical to `who-fucked-up/+page.svelte`. But SonarCloud's automatic analysis has no Svelte
parser: every SonarCloud finding in this repository's history is in a `.ts` file, and there is no
`sonar-project.properties` widening that. Refactoring the stylesheets would have been a large,
risky change to three working routes that fixed nothing.

The real duplication was twelve byte-identical lines of TypeScript between the new
`a focused mode page turns its verdict into a coloring page` test and the existing
`a page made on a mode route can be saved and found in the home vault` test — the same closing
sequence, down to the `'For the group chat'` literal, because the new test was written from the old
one. Extracted as `makePageAndKeepIt`, which both now call. That is the same fix, for the same
gate, that `makeToolkitPage` already represents in this file.

Measured rather than assumed afterwards: an 8-line n-gram scan across the changed `.ts` files
against the rest of `src/`, `tests/` and `contracts/` reports no remaining cross-file or internal
duplication in new code. The two internal repeats it still finds in `tests/e2e/smoke.spec.ts`
(lines 573 and 851) are in tests that predate this pull request, so they are not new code.

Evidence on this head: check 0 errors / 0 warnings, lint exit 0, test 1187 passed | 1 skipped,
build exit 0, playwright 26 passed, `npm run verify` exit 0 across all eight stages.

## Round 3 — the Codex round on 75b0017: two accepted, one declined

Both accepted findings were measured before they were fixed and mutated after, rather than taken on
the reviewer's word or on mine.

  - **The dedication survived a replacement saying on /m/random.** Real. `/random` already solves
    this and its comment states the rule exactly: a mode that asks nothing returns a new subject
    every tap, so a dedication chosen for the previous saying must not ride along; a mode that asks
    a question is re-asking about the same situation, so its dedication still belongs. The generic
    page had no split at all. Now it awaits the installed verdict and clears only for the inputless
    mode. Both halves are asserted, because the fix is a split and not a blanket clear. Removing the
    clear fails the test with Received "For the group chat".

  - **The ambient decoration widened the document on a phone.** Real, and measured before being
    believed: at a 390px viewport `document.documentElement.scrollWidth` was 422 against a
    clientWidth of 390 — exactly the decoration's `right: -2rem` overhang — so the page could be
    panned sideways into blank space. `overflow-x: clip` on `.page` (not `hidden`, which would make
    it a scroll container on both axes) takes it to 390. The same 32px was measured on all three
    standalone mode routes, which carry the identical decoration, so the one-line fix went to all
    four rather than leaving the same defect next door. Removing it fails the test with
    "/m/who-fucked-up pans sideways by 32px".

The declined finding is the seam-workflow P1, answered on its thread and deliberately left open.
The evidence for the classification in this file's header is unchanged and is checkable: no file
under contracts/, probes/, fixtures/, src/lib/mocks/, tests/contract/, src/lib/adapters/ or
src/lib/seams/ is in the diff, and `verdict-page-state.svelte.ts` — the module that actually calls
those adapters — is not modified by this pull request at all.

Evidence on this head: check 0 errors / 0 warnings, lint exit 0, test 1187 passed | 1 skipped,
build exit 0, playwright 28 passed, `npm run verify` exit 0 across all eight stages.

## Round 4 — the close-out head (PR #296), and why lint/build/e2e were re-run for a docs commit

This head is `main` at `1dab4cf` (the merge of PR #295) plus one documentation commit. The `src/`
and `tests/` trees are byte-identical to that merge, so lint, build and the e2e suite were always
going to produce the same result they produced on `f81802d`.

They were re-run anyway, and the reason is recorded rather than re-derived: run 3 took a P1 for
leaving exactly these three artifacts describing an earlier head on exactly this argument.
`AGENTS.md` requires `npm run check`, `npm run lint`, `npm test` and `npm run build` **before every
push** — the rule is about the head being pushed, not about whether the inputs are believed to have
changed. An artifact that predates the head cannot prove the head passed, however sound the
reasoning is that it would. "I am sure it still passes" is a claim; running it is a measurement.

The e2e suite is scoped by `AGENTS.md` to user-facing changes and this commit is not one. It was run
regardless, because the argument for skipping it is the same argument that was already wrong once
above.

`proof-tape.json` was re-rendered after all three so the tape sees them; `cipher-gate.json` is again
the only file it marks as predating the run, correctly — no seam artifact is in this diff either.

Evidence on this head: check 0 errors / 0 warnings, lint exit 0, test 1187 passed | 1 skipped,
build exit 0, playwright 28 passed, `npm run verify` exit 0 across all eight stages.

## Round 5 — the Codex round on the close-out head `9375e2b`

Three findings, all about this log or its evidence rather than about the app. Two accepted, one
declined on new evidence.

  - lint/build/e2e predating the head: already fixed in `ad171be`, pushed before the review landed.
  - The clock follow-up named `getMonthKey()` only, while `getWeekNumber()` (meechie-studio.ts:229)
    selects the other two of the three modes the home page shows. Superseded in the log.
  - The seam workflow, argued a third time. Declined again, but on two new measurements rather than
    a third restatement: every file the rebuild added or changed contains zero direct host access
    (no fetch/localStorage/Date.now/new Date/crypto/performance/document/window/navigator), and
    `npm run rewind -- --seam <name>` exits 0 for every seam on the paths the page reaches. That
    list has been wrong five times before it was right: six seams, twelve, fourteen, fourteen with
    two canonical rows, and now **nineteen rewind runs covering fourteen seams** — the six the page
    touches directly (MeechieToolSeam,
    SpecValidationSeam, OutputPackagingSeam, CreationStoreSeam, SessionSeam, ClockSeam), the six
    reached server-side inside /api/generate (PromptAssemblySeam, ImageGenerationSeam,
    ImageProviderConfigSeam, SafetyPolicySeam, DriftDetectionSeam, RateLimitSeam), and the two
    nested inside meechieToolAdapter.respond (MeechieVoiceSeam, ProviderAdapterSeam). **Five** of
    them are additionally run under their canonical "(self-contained)" rows, because rewind
    resolves the first matching row in docs/seams.md and a legacy row precedes the canonical one
    for MeechieVoiceSeam, PromptAssemblySeam, SpecValidationSeam, MeechieToolSeam and
    DriftDetectionSeam — production imports the self-contained adapter in every one of those five.
    **What the bare run costs differs by seam**, and saying "the bare command verifies a seam the
    code does not use" of all five overstated it — that holds for two. MeechieVoiceSeam
    (meechie-voice.adapter.ts builds its own adapter over seams/meechie-voice-seam/voice-pack) and
    DriftDetectionSeam (drift-detection.adapter.ts is standalone) are genuinely separate
    implementations. The other three are compatibility shims over the canonical adapter —
    prompt-assembly.adapter.ts delegates to it, and spec-validation.adapter.ts and
    meechie-tool.adapter.ts are one-line re-exports — so their bare runs exercise the same
    production code through a narrower suite: incomplete canonical coverage, not the wrong seam.
    The per-seam split with test counts is in plan.md. rewind is AGENTS.md's own instrument for
    seam-scoped verification when full verify is not required.

Evidence on this head: check 0 errors / 0 warnings, lint exit 0, test 1187 passed | 1 skipped,
build exit 0, playwright 28 passed, verify exit 0 across all eight stages, and 19 rewind runs
whose recorded exit codes were all 0, matching the 19 rewind-*.txt artifacts in this
directory. This sentence was written at 2026-09-04T20:56:38Z, after those runs completed — an earlier
revision declared the rewind result in a paragraph saved a minute *before* the first rewind
executed, which made it a prediction rather than a record.

## Round 6 — the Codex round on `0deb9b0`: three P2s, all "the correction stopped one file short"

Accepted all three. None is in the application, which is byte-identical to the merge of PR #295.

  - The ImageGenerationSeam fixture waiver in DECISIONS.md said "a fifth screen ... four others",
    where the same file (line 87) and plan.md both say sixth. Counted from the base tree rather
    than from memory: `git grep -n "api/generate" 210b301 -- src/` finds three modules that POST to
    it — studio-state.svelte.ts:668 (home), MeechieTools.svelte:283 (/meechie), and
    verdict-page-state.svelte.ts:480 (the three standalone mode routes). Five screens; /m/<slug> is
    the sixth. Third wrong exposure count in this close-out; the waiver now enumerates the five and
    names which endpoint the count belongs to, because /api/tools and /api/generate have different
    consumer sets and I kept carrying a number between them.
  - `npm run cipher:gate` was run and claimed as validation for that waiver but was missing from
    the plan's command list — and it is NOT in the verify chain (package.json:33 vs :26), so the
    plan as written could not reproduce the claim. Both standalone gates are now listed literally.
  - The "wrong seam" narrowing from round 5 was applied to plan.md and not to this file, which went
    on asserting the strong version at line 189. Fixed above, in place.

## Round 7 — the Codex round on `4e3e54c`: four findings, two of them in evidence I had just added

Accepted all four. Two are defects in the round-6 fixes themselves, which is the useful part: the
artifact added to make a claim checkable broke the check, and the paragraph asserting an order
asserted one that had not happened.

  - **P1: the copyable command block was not copyable.** Three of the nineteen lines had no space
    before the inline `#`, so a shell reading
    `--seam "DriftDetectionSeam (self-contained)"# rewind-...` passes the seam name as
    `DriftDetectionSeam (self-contained)#` and rewind's exact-row lookup fails. The block exists
    *because* quoting is load-bearing for those five names, so the defect landed precisely where it
    does the most damage. Fixed, and verified by parsing rather than by looking: every one of the
    nineteen lines now yields exactly two arguments, `--seam` and the seam name with its
    parenthesised suffix intact.
  - **P1: the new exit-code log had no file header.** AGENTS.md's File Header Requirement is
    unconditional and this file is not exempt for being small or hand-built. It now states what it
    records, why (the per-seam artifacts carry a suite's output but not the command's exit status),
    and how it flows into this summary.
  - **P2: the file was named into the pattern it was being counted against.** `rewind-exit-codes.txt`
    matches `rewind-*.txt`, so the directory held twenty files while this summary claimed nineteen —
    the artifact added to make the count checkable was the thing that falsified it. Renamed to
    `seam-rewind-exit-codes.md`, outside the pattern. `ls rewind-*.txt | wc -l` is 19 again.
  - **P2: the chronology predeclared the last command.** The paragraph said it was written at
    21:08:15Z "after every run above completed", listing `proof:tape` above it — but
    proof-tape.json's generatedAt was 21:08:36Z, twenty-one seconds later. The order is fixed below
    rather than the sentence reworded, because the sentence was describing an order that cannot
    exist: see the note after the list.

## Round 8 — the Codex round on `6de7232`: three findings, one of them self-inflicted an hour ago

  - **P1: the round-7 refresh destroyed the headers on lint.txt, build.txt and e2e.txt.** Those
    three files carried full Purpose/Why/Info-flow prologues at `1dab4cf`. Re-capturing them with a
    one-line `{ echo "# npm run lint — started ..."; npm run lint; }` overwrote the file, header
    included, and I did not look at what I had replaced. **This happened in the same commit whose
    second finding was a missing file header**, which is the clearest possible demonstration that
    reading the rule is not the same as applying it to the file in front of you. Restored, with the
    capture now writing the header explicitly so a refresh preserves it by construction rather than
    by my remembering.
  - **P1: the rewind summary was stored as .txt.** `docs/AGENTS.md` reserves `.txt` for raw terminal
    dumps and requires summaries of validations to be Markdown. The file calls itself a summary in
    its own header, so the rule applies by my own description of it. Now
    `seam-rewind-exit-codes.md`, with the runs as a table and an HTML-comment header.
  - **P2: the plan's Files entry collapsed all evidence into `docs/evidence/2026-09-04/`.** Every
    path is now listed, split by what wrote it.

**The consistency question this raises, stated rather than quietly skipped.** If a validation
summary must be Markdown, this file — `verify-chain.txt` — is one too, and has been for every run in
this directory and in 2026-09-03's. It is not renamed here, and the reason is not that the finding
was scoped to the other file: `verify-chain.txt` is named in five `DECISIONS.md` entries, in a
comment in `scripts/proof-tape.mjs`, and across dated evidence folders from earlier runs. Renaming
it is a repository-wide convention change that would silently invalidate those references, which is
a bigger decision than a close-out pull request should take on its own. It is recorded as a
follow-up in WORST_TO_BEST_LOG.md instead. The other file was two commits old and mine alone.

## Round 9 — the Codex round on `6cdd02d`: three P2s, all of them claims that were true-ish

  - **The e2e header said two browser-only guards; there are three.** The 390px `scrollWidth`
    measurement needs layout, and jsdom does none, so no unit test can make that assertion at all.
    Corrected here and in the header of e2e.txt, since the same sentence lived in both — the
    now-familiar failure of fixing one copy. The dedication-split test is deliberately still
    excluded, with the reason stated: it is component state a DOM test could assert.
  - **The plan claimed `assumption:alarm` "sees the same entries before and after".** False.
    `blockedSeams` is byte-identical (`ImageGenerationSeam`, `RateLimitSeam`, `WigTryOnSeam` at
    `1dab4cf` and here), but `assumptions` goes 11 -> 12, because the fixture waiver is itself a new
    Assumption. The narrow claim is the true one and is what the plan now says.
  - **A follow-up numbered 10 where the list ends at 7.** The close-out carries Run 3's four forward
    and adds 5, 6, 7; numbering the new one 10 invented two phantom items a later run would go
    looking for. Renumbered to 8 by an appended correction, since the log is append-only.

## Round 10 — the Codex round on `234cc4c`: a validation plan that could not validate

Two findings, and both are about proof that looks like proof.

  - **P1: the fixture waiver's validation plan was unexecutable.** It said "refresh from a live
    probe, then re-run the rewind and the contract tests". But `probes/image-generation.probe.mjs`
    writes `fixtures/image-generation/sample.json` and `fault.json`, and **nothing reads them** —
    `grep -rn "fixtures/image-generation" src/ tests/ scripts/ contracts/` returns zero hits, while
    both suites import `src/lib/seams/image-generation-seam/fixtures.ts`. Following the plan would
    have spent a billable live image call and produced a green rewind that never touched the
    regenerated data: a validation that cannot fail for the reason it claims to test. Measured
    across the whole tree rather than assumed for this one seam — of the fourteen directories under
    `fixtures/`, `image-generation` is **the only one with no consumer**; the other thirteen have
    one to four. The waiver's plan now has the transcription step that makes it work, and the
    orphaned capture directory is recorded as a repository follow-up rather than fixed inside a
    documentation change.
  - **P2: `cipher:gate` exit 0 was cited as validating the waiver.** It does not.
    `scripts/cipher-gate.mjs` selects a Cipher Gate block from DECISIONS.md; this close-out adds
    none, so it selected the existing 2026-09-04 block for the Quote Vault's ClockSeam,
    AppOriginSeam and PageVisibilitySeam, confirmed that block's evidence paths still exist, and
    exited 0 — which `cipher-gate.json` on this head records plainly. **That is the third borrowed
    authorization in this close-out and the first one wearing a green gate**, which is what makes it
    worth recording: a passing check is only evidence for the thing it actually checked. The claim
    is removed; what the run legitimately shows is that an unrelated earlier entry's evidence has
    not gone missing.

## Round 11 — the Codex round on `e8bb390`: the repaired validation plan was still unexecutable

Three findings, two P1. The two P1s are both about the plan written *in response to round 10*, which
means round 10's repair was itself wrong — the third round running in which a fix needed fixing.

  - **No captured success can reach the mock.** `fixtures.ts` exports a *request* and a fault and
    nothing else; `mock.ts:10` imports only the fault and its `sample` scenario synthesises an SVG
    through `buildSvg` rather than replaying a provider response; `tests/contract/
    image-generation.test.ts:32` uses its own hard-coded `xaiSampleResponse`. So "transcribe the
    captures into fixtures.ts" — round 10's repair — had nowhere to put a captured success.
  - **The probe cannot capture a live fault at all.** It throws on any non-2xx (`:138-148`), so a
    real provider failure aborts the run rather than being recorded, and the `fault.json` it writes
    afterwards on the *success* path is a hard-coded `PROMPT_MISSING_REQUIRED_PHRASES` (`:224`) —
    a different code from the canonical `IMAGE_HTTP_ERROR` fixture (`fixtures.ts:15`).
  - **P2: the command list omitted the standalone `npm run proof:tape`**, which is the
    ordering-sensitive final step this very file documents. Added, with the reason.

**The waiver's Validation field now says BLOCKED rather than offering a third plan.** Three review
rounds produced three unexecutable plans; the honest reading is that no plan expressible in
documentation can validate this waiver, because the seam's refresh path needs three code changes
first. Those are enumerated in the entry and recorded as follow-up 9. The waiver rests on the
diff-unchanged argument alone, which is a weaker position than it looked two rounds ago and is now
stated as such.

**One thing this round's edit broke, caught by the gate rather than by me.** Rewriting the Validation
field as an indented list made `npm run verify` exit 1. `scripts/assumption-alarm.mjs:61-64` reads an
Assumption block as consecutive lines starting with `  - ` and stops at the first line that is not,
so a nested list truncated the block before `Status` and the entry parsed as incomplete. The field is
one line again. Worth recording twice over: the gate did exactly its job, and a formatting choice in
a governance document is a functional change to what the gate can read.

## Round 12 — the Codex round on `6a1681b`: "blocked" was an overcorrection

One P1, and it lands squarely. `AGENTS.md`'s completion checklist permits a fixture waiver only
"with the assumption being made, the assumption documented, and **a stated plan for later
validation**". Round 11's field named three prerequisite code changes and stopped there — it said
what would make validation *possible* and never said what to run afterwards. That is not a plan for
later validation, and `plan.md` meanwhile still claimed the waiver had one.

**The distinction I collapsed:** what could not be written was a plan that runs *today*. A plan
conditional on the three repairs was always writable, and round 11 overcorrected from "this plan is
fiction" to "no plan can be written", which is a different and stronger claim than the evidence
supported. Both errors have the same shape — a sentence that sounds decisive and is not checked.

The field now carries the seven-step post-repair sequence: probe, refresh `fixtures.ts` from the
captures, **red proof on the fault fixture before it is accepted** (captured as
`image-generation-fault-red-proof.txt`, the step a hurried run skips and the only one that proves the
refreshed fault can fail), both suites, the rewind, `verify` plus a Cipher Gate for the repairs, the
`docs/seams.md` probe date, and finally marking the Assumption Resolved with its evidence paths.
`plan.md` no longer claims a plan exists unconditionally; it states the condition.

## Round 13 — the Codex round on `15c6e22`: four P1s, and the end of writing command sequences

All four are against the seven-step validation plan written one round earlier. Each was verified in
the code before being accepted:

  - **Wrong model.** The probe hard-codes `model: 'grok-imagine-image'` (`:132`); production resolves
    `grok-imagine-image-2.0` through `ImageProviderConfigSeam`
    (`image-provider-config-seam/fixtures.ts:10`). A live call would characterise a model the shipped
    adapter never uses.
  - **Wrong shape.** The probe writes `format`/`mimeType`/`data`/`encoding`/`modelMetadata`
    (`:157-190`); `contract.ts:19-29` requires `{ images: [{ id, url?, b64? }], rawModelInfo,
    timingMs }`. A capture cannot be loaded without inventing fields.
  - **Wrong order.** Steps 6 and 7 ran the gates before updating `docs/seams.md` and the Status;
    `scripts/assumption-alarm.mjs:93-106` computes `blockedSeams` from exactly those two files, so
    the committed evidence would certify the pre-resolution tree.
  - **A red proof that cannot turn red.** `image-generation-seam/test.ts:32-41` compares
    `result.error` against the same `imageGenerationFaultFixture` that `mock.ts:21-25` returns. It is
    **green by construction**, and pointing it at a refreshed fixture keeps it green. This is the
    finding with teeth beyond this PR: the seam has a fault test that cannot fail.

**What changed structurally.** Five rounds produced five command sequences and a reviewer falsified
each by reading one file further — wrong destination, wrong fault, wrong model, wrong shape, wrong
ordering. The common factor is not carelessness about any one file: **a command sequence predicts the
tree it will run in, and this one does not exist yet.** The Validation field now states six measured
defects and seven acceptance criteria — properties that must hold in whatever tree the repair
produces — instead of commands. Criteria are checkable against a future state; commands only look
like they are.

## Round 14 — the Codex round on `46af6ba`: the waiver waived one gate and behaved as if it waived three

One P1, and it is the structural point the previous five rounds were circling without naming.

`AGENTS.md:102-104` lists three **separate** completion items, and only the first admits a waiver:

  - "Fixtures are fresh (<= 7 days) **or waiver recorded**" — waived here, under the rule that
    permits it.
  - "Mock loads fixtures by scenario (no logic shortcuts)" — **fails.** `mock.ts:12-35` synthesises
    the `sample` response with `buildSvg`.
  - "Fault fixture fails before adapter work (red proof)" — **fails.** `test.ts:32-41` compares the
    mock's error against the fixture the mock returns, so no red proof exists or can.

This entry's own Validation field had already established both, and its Status still read "Waived
for PR #295 and its close-out" — a freshness waiver behaving as though it covered all three. **No
waiver of any kind exists for the second and third**, and writing one is not this run's to write.

Both defects **predate PR #295**: they are properties of the seam at base `210b301`, not things that
PR introduced. What PR #295 did was route `/m/<slug>` to `/api/generate`, which is what brought the
seam into scope at all. "Pre-existing" explains them and does not excuse them — the checklist is
about the seam the change exposes, not about who broke it.

The Status field now says freshness only, names the two failing items, and states that waiving them
is an owner decision. **This close-out therefore does not claim the completion gate is met for
`ImageGenerationSeam`, and the thread is left open rather than resolved**, alongside the
seam-workflow P1. Two rulings are now outstanding, and the second one bears directly on whether this
run may call itself done.

## Round 15 — the Codex round on `f2de10e`: back to the original entry

Three P2s, and two of them are against the **Run 4 entry written at the start of this run** — the
first findings in twenty-one rounds to reach that far back.

  - **"The last Svelte 4 component" was not the last.** `MeechieTools.svelte` has seven `on:click`
    handlers and no runes at all, so it is still in legacy mode. The true claim is narrower:
    `MeechieModePage.svelte` was the last legacy component among the *mode pages*.
  - **"One edit adds a mode" holds only for already-supported tools.** `mode-catalog.ts:199-201`
    skips any mode whose `toolId` is missing from `FIELDS_BY_TOOL` or `BUILD_INPUT_BY_TOOL`, while
    `StudioHero.svelte` still renders its link — so the home page gains a link to a 404. `lineup`,
    `horoscope` and `meechie_explains` are valid tool ids in exactly that position. It cannot ship
    silently, because `tests/unit/mode-catalog.test.ts` fails on such an addition, and that is worth
    stating in the change's favour — but a failing test is not a working page, and the entry
    promised the second.
  - **"Three open Assumptions" was two plus a waiver.** Only the 2026-08-24 provider and 2026-08-26
    `RateLimitSeam` entries have Status `Open`; the 2026-05-14 `ImageGenerationSeam` entry named as
    the third is itself `Waived`, and what this change adds is a new 2026-09-04 entry marked "Waived
    for fixture freshness ONLY". An audit searching for open Assumptions would not find the third.

Both of the first two are the same species: **a sentence that made the work sound better than it
was**, in the document whose purpose is to tell the next run what is true.

## Round 16 — the Codex round on `bdfe0cf`: two acceptance criteria that contradicted each other

  - **P1: criteria C and D could not both hold.** C requires captures "loadable without
    transformation"; D said the contract test should load them in place of its private
    `xaiSampleResponse`. But that test stubs `fetch`, so it must be handed the **provider-wire**
    shape `{ data: [{ url, b64_json, revised_prompt }] }` that
    `image-generation-seam/index.ts:17-20,145` parses — while the mock and the seam contract want the
    **normalized** `{ images, rawModelInfo, timingMs }`. Handing a canonical seam result to `fetch`
    makes the adapter report an empty response; reshaping it in the test is the transformation C
    forbids. There are genuinely two shapes here, and one criterion was quietly assuming one shape.
    Now: the probe captures **both** — the raw provider response as received, and the normalized
    result the adapter produces from it — the contract test loads the wire capture, the mock loads
    the normalized ones, and nothing transforms anything. The adapter is the only thing that maps
    between them, which is precisely what its test exists to check.
  - **P2: the one-edit promise was corrected in the log and left standing in `DECISIONS.md:131`.**
    Correction 21 appended to `WORST_TO_BEST_LOG.md` only. This is the *same* fixed-only-the-quoted-
    copy failure named in rounds 12 through 14 — in a correction whose own stated lesson was "grep
    for the claim, not the line number", and I did not grep. Grepped this time: two hits, one the
    real claim and one an unrelated idiom at `DECISIONS.md:444`.

## Round 17 — the Codex round on `8c9ff7c`: I fixed D and left C saying the opposite

One P1, and it is the previous round's repair caught half-done. Round 16 rewrote criterion **D** to
keep the provider-wire and normalized shapes distinct — and left criterion **C** still requiring
"both captures are written in the canonical contract shapes". The wire object `{ data: [...] }` is
not an `ImageGenerationSeam` contract value and never becomes one until the adapter maps it, so C
and D still contradicted, and a maintainer could still have paid for a live probe and produced
captures that satisfy neither.

**The conceptual error, now named in the entry rather than patched around:** there is no single
canonical shape in this seam. There are two, and each has exactly one consumer. C now says each
capture is written in the shape *its own consumer* requires and is validated against *that* shape —
the wire capture against the adapter's `payload.data` reader (`index.ts:17-20`), the sample and
fault captures against `contract.ts:19-29`. "Without transformation" keeps its one real meaning:
each consumer loads its capture verbatim, with no reshaping at load time.

This is the third consecutive round in which a repair needed repairing, and the second in which the
half I did not edit contradicted the half I did.

## Round 18 — the Codex round on `8355b6a`: the error path had no consumer and no validator

Two P1s. Both are about the **fault** half of the criteria, which every previous round's attention
had gone past — the success path got six rounds of scrutiny and the error path got none.

  - **The live error capture would have had no reader.** Criterion D replaced only the *success*
    `xaiSampleResponse`; the adapter's HTTP-failure test builds
    `new Response('rate limited', { status: 429 })` inline at
    `tests/contract/image-generation.test.ts:78-96`. So criterion B's real non-2xx response could be
    recorded and then read by nothing, every test still green, with no check that the adapter maps
    the provider's *actual* error body to the normalized fault. D now sends the wire-error capture
    to that test, and B says plainly there are **three** captures — wire-success, wire-error,
    normalized — not two.
  - **The fault capture cannot be validated at all today.** Criterion C pointed the normalized
    captures at `contract.ts:19-29`, which contains only `GeneratedImage` and
    `ImageGenerationResult`; the `ImageGenerationError` union is at `:35-42`. And `validators.ts`
    exports only request, image and result schemas — **there is no error validator in the seam.**
    So "validate the fault capture" was unsatisfiable, against a line range that did not even hold
    the type. C now names the right range and makes adding `imageGenerationErrorSchema` /
    `validateImageGenerationError` part of the repair.

Worth recording as a pattern rather than two more line-number fixes: **six rounds refined the
success path and none touched the fault path**, because every previous finding happened to be
phrased about a success case and I followed the reviewer's frame each time instead of walking both
halves of the contract.

## Round 19 — the Codex round on `ef711f7`, and a reordering that should have happened earlier

Three findings: two P1, one P2.

  - **P1: the full chain did not cover the final tree.** Every previous round ran `npm run verify`
    first and wrote this summary afterwards, so the committed chain result was evidence for the tree
    *before* the summary existed. Only `assumption:alarm` and `proof:tape` ran after it — not
    `audit:gate`, `check`, `test`, `shaolin-lint`, `seam-ledger` or `clan-chain`.
    `AGENTS.md:195-196` asks for the complete set before every push, and "complete" means on what is
    being pushed.
  - **P1: four capture values, counted as three.** Round 24 corrected "two" to "three"; it is four —
    provider-wire success, provider-wire error, normalized result, normalized error — each with
    exactly one consumer, and the two normalized ones are disjoint arms of `Result<>` that one
    "normalized capture" cannot supply.
  - **P2: correction 24's synthesis was false.** It claimed no round since 16 had examined the fault
    path; corrections 17, 19 and 20 all did. The true statement is narrower: the fault path's
    *wiring* — who consumes its captures, what validates them — went unexamined, not its existence.

## The ordering, changed this round and stated before the runs it describes

  1. every edit, **including this section**
  2. npm run verify        — the eight-stage chain, on the final tree. Its result is in
                             verify.txt, test.txt and chamber-lock.json, which are written after
                             this paragraph. **This file no longer asserts that result**, for the
                             same reason it carries no hand-typed timestamp: the artifact is the
                             claim, and prose about an artifact written before it is a prediction.
  3. npm run lint          -> lint.txt        ) after the chain, so they postdate chamber-lock.json
  4. npm run build         -> build.txt       ) and are not marked as predating the run by
  5. npx playwright test   -> e2e.txt         ) markArtifactsPredatingRun
  6. 19 rewind runs        -> seam-rewind-exit-codes.md, each exit code appended as it finishes
  7. npm run cipher:gate, npm run assumption:alarm   — the two standalone gates
  8. npm run proof:tape    — LAST, so the tape inventories every file above, this one included

Steps 3-8 all postdate step 2, which is the point: the chain runs on the tree being pushed, and
everything after it is either an artifact the chain does not read or the tape that inventories them.
Where to look for each result: `verify.txt` and `test.txt` for the chain, the `EXIT=` line at the
foot of `lint.txt` / `build.txt` / `e2e.txt`, the exit column in `seam-rewind-exit-codes.md`, and
`proof-tape.json`'s `generatedAt` for the tape.

**The two rulings from round 14 remain outstanding.** The completion gate for `ImageGenerationSeam`
is not met, the freshness waiver covers one of its three items, and this close-out is not merging on
its own judgement.
