# Red proof for the `pendingTitleRestore` guard in PrintPageButton.svelte, 2026-09-07.
#
# The guard was removed (`if (pendingTitleRestore === null)` -> `if (true)`, restoring the
# pre-fix behaviour of capturing and registering on every click) and the test re-run. It fails,
# which is what makes the test worth having: it is measuring the guard, not describing it.


Running 1 test using 1 worker

  ✘  1 [chromium] › tests/e2e/print.spec.ts:257:1 › the print job is named after the page, and a second print does not strand the tab name (6.7s)


  1) [chromium] › tests/e2e/print.spec.ts:257:1 › the print job is named after the page, and a second print does not strand the tab name 

    Error: [2mexpect([22m[31mreceived[39m[2m).[22mtoBe[2m([22m[32mexpected[39m[2m) // Object.is equality[22m

    Expected: [32m"Meechie's Coloring Book Studio"[39m
    Received: [31m"Receipt Energy"[39m

    Call Log:
    - Timeout 5000ms exceeded while waiting on the predicate

      286 |
      287 | 	await page.evaluate(() => globalThis.dispatchEvent(new Event('afterprint')));
    > 288 | 	await expect.poll(() => page.title()).toBe(appTitle);
          | 	^
      289 | });
      290 |
        at /home/user/meechiescoloringbook/tests/e2e/print.spec.ts:288:2

    Error Context: test-results/print-the-print-job-is-nam-b7c88-oes-not-strand-the-tab-name-chromium/error-context.md

  1 failed
    [chromium] › tests/e2e/print.spec.ts:257:1 › the print job is named after the page, and a second print does not strand the tab name 
