# Purpose: Record the whole verification sequence for this head in one place — the eight-stage
#          chain, the standalone checks run around it, and what each artifact in this folder does
#          and does not prove.
# Why: The individual artifacts each show one command's output. Nothing else in the folder says in
#      what order they were produced, which of them the chain wrote and which were run by hand, or
#      why a file carries a freshness marker.
# Info flow: npm run verify + standalone checks -> their evidence files -> this summary -> review.
#
# Run 5 of the worst-feature routine — the wig try-on becomes a shop you can browse and a look you
# can keep. Head 6c5a151, captured 2026-09-05T00:49:01Z.

## The chain (npm run verify) — exit 0

1. npm run audit:gate      -> found 0 vulnerabilities
2. chamber-lock.mjs        -> chamber-lock.json      (seam artifact presence)
3. verify-runner.mjs       -> verify.txt, test.txt   (npm run check + npm test)
4. shaolin-lint.mjs        -> shaolin-lint.json      (evidence freshness)
5. assumption-alarm.mjs    -> assumption-alarm.json  (Assumption entry coverage)
6. seam-ledger.mjs         -> seam-ledger.json/.md   (seam coverage ledger)
7. clan-chain.mjs          -> clan-chain.json/.md    (clean vs dirty seams)
8. proof-tape.mjs          -> proof-tape.json/.md    (plain-English summary)

## Standalone, run by hand on this head

- npm run lint   -> lint.txt   (exit 0; eslint prints nothing on success)
- npm run build  -> build.txt  (exit 0)
- npm run cipher:gate -> cipher-gate.json (exit 0)
- npm run rewind -- --seam WigCatalogSeam -> rewind-wig-catalog-seam.txt (27 passed)
- npx playwright test -> e2e.txt (30 passed; baseline on main at 1dab4cf was 28)

## Results on this head

- npm run check: 0 errors, 0 warnings
- npm test: 1238 passed, 1 skipped (baseline on main at 1dab4cf: 1187 passed, 1 skipped)
- npm run build: exit 0
- npx playwright test: 31 passed
- npm run verify: exit 0, all eight stages

## The Cipher Gate entry, and what it claims

- cipher-gate.json is now in this folder, and DECISIONS.md carries a Cipher Gate entry dated
  2026-09-05 for WigCatalogSeam. It was added after a review finding argued that moving the UI from
  bypassing the seam to consuming listWigs() makes seam outcomes newly observable, and AGENTS.md
  says to treat doubt as a seam change.
- The entry does not claim the seam changed, and neither does this file. No file under contracts/,
  probes/, fixtures/, src/lib/mocks/, tests/contract/, src/lib/adapters/ or src/lib/seams/ is in the
  diff; all six of WigCatalogSeam's artifacts pre-date this work; no contract shape moved. The seam
  returns exactly what it returned before for the same input.
- rewind-wig-catalog-seam.txt is the positive evidence for that: the seam's contract still verifies
  on this head, 27 tests passed.
- wig-catalog-seam carries a documented manual probe with no runProbe export (docs/seams.md records
  N/A in its probe-date column), which pre-dates this change. So the seam's automated evidence here
  is its contract test via rewind plus chamber-lock's artifact-presence gate, not a fresh probe run.

## What this evidence does not prove

- The four behavioural guards in this change are invisible from the outside, so a green suite does
  not establish them. Each was confirmed by deleting it and watching a named test fail; those
  mutation runs are recorded in WORST_TO_BEST_LOG.md under Run 5, not in this folder.
- SonarCloud's duplication gate reports a percentage and no location and cannot be run locally. An
  n-gram scan over the changed .ts files was used instead, before each push. It removed a 10-line
  repeat between two new tests before the first push (the gate then passed at 0.0%), and removed the
  same shape again after the Codex round added two more staleness tests. The three blocks it still
  reports were each checked against the diff hunks and are all in unchanged lines.
- SonarCloud reports 1 new issue beside its passed gate whose location is not readable from this
  environment: the check run's annotation text is empty, no inline comment was posted, and the
  network policy returns 403 for sonarcloud.io. It is not gate-blocking, not a security alert, and
  not duplication or coverage. Recorded unresolved rather than guessed at.
