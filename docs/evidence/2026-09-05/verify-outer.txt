# Purpose: Capture the OUTER 'npm run verify' command's own output and exit status.
# Why: verify.txt holds only the inner verify-runner stage, despite its name. Without this
#      file the audit:gate result and the chain's exit status were asserted in
#      verify-chain.txt rather than captured, which a review flagged as unauditable.
# Info flow: npm run verify -> this transcript -> verify-chain.txt's summary of it.
# Not in proof-tape: this is the transcript OF the run that writes the proof tape, so it is
#      written after the chain returns and is absent from that chain's inventory. Capturing it
#      in place instead made proof-tape record 1901 bytes for a 1908-byte file - it had not yet
#      seen this file's own closing exit line. The convention and capture order are recorded
#      in docs/evidence/README.md so a future run does not have to rediscover them.


> meechies-coloringbook@0.1.0 verify
> npm run audit:gate && node scripts/chamber-lock.mjs && node scripts/verify-runner.mjs && node scripts/shaolin-lint.mjs && node scripts/assumption-alarm.mjs && node scripts/seam-ledger.mjs && node scripts/clan-chain.mjs && node scripts/proof-tape.mjs


> meechies-coloringbook@0.1.0 audit:gate
> npm audit --audit-level=high

found 0 vulnerabilities
